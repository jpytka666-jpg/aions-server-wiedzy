{
  "aions_core/server/cbms_outline.py": "This file implements a progressive disclosure system for retrieving information from the CBMS (Code Book Memory System) storage. The mechanism splits knowledge retrieval into two phases to save context space: first, a lightweight skeleton of available chunks (summaries without full content), and second, full detailed content for only the selected chunks. The skeleton includes identifiers, concepts, character counts, access statistics, and summary lines, while actively tracking whether chunks are stored (active tier) or archived (parked tier). The system accepts search queries, concept filters, and budget constraints in characters to trim results by relevance score, and uses tokenization to skip common words in matching. The tool reads from disk directories containing JSON chunk files, calculates incoming reference degrees to weight important chunks, and returns results formatted as JSON or human-readable tables. Callers can request outline summaries for planning or drill deep into specific chunks for full content, making it possible to fit thousands of chunks' metadata into a small context window.",
  "control_plane/agents/agent_registry.py": "This file provides a discovery and management system for autonomous agent roles in the AIONS system, similar to how skills are registered. Each agent is defined by a folder containing an agent.json configuration file, which the registry automatically discovers from an agents_lib directory tree without requiring code changes. The registry loads and validates each agent definition against a schema, storing valid agents in memory by their identifier while collecting any schema validation errors. The system provides methods to look up a specific agent by its identifier, list all loaded agent IDs, and retrieve a summary of discovery results showing how many agents loaded successfully and what errors occurred. This plug-and-play architecture allows adding new agent roles by simply creating a folder and configuration file without modifying core system code.",
  "control_plane/blends/__init__.py": "This file is an empty Python package marker for control plane blend learning command-line entry points.",
  "control_plane/blends/write_conclusion.py": "This script completes the analysis of a learning blend by writing out conclusions and optionally exporting the result as a knowledge chunk for the CBMS system. It takes a blend identifier (either a path or a name from seed or conclusion directories) and runs mistake analysis and conclusion generation through helper functions, then updates the blend's manifest while optionally creating a JSON file suitable for import into the knowledge storage. The script supports output path override and can write results as JSON to stdout for automation. If CBMS export is requested, it creates a chunk file and appends an ingest note marking the chunk as pending for manual import into the production knowledge database, ensuring new learned material stays offline until manually approved. The tool integrates offline blend learning with the broader knowledge system while maintaining safety by preventing automatic production database updates.",
  "control_plane/llm_adapter.py": "This file is the mouth layer that lets AIONS speak through a local language model (Ollama, Bielik, or Phi), injecting knowledge from CBMS, operator profile, and conversation history into the prompt. The adapter is thin and focuses only on response generation, never touching the rule-based planner or decision logic. Configuration comes through environment variables controlling the provider (local Ollama versus OpenAI versus disabled), model identifiers, timeouts, and knowledge base paths. The system builds complete prompts by combining a system instruction, operator context (goals and active cases from a profile file), semantic search hits from the knowledge database, and optional enrichment from the KORZENIEC cache layer. It supports both a simple interface returning just text response and a detailed interface returning metrics about generation speed, token count, and which knowledge chunks were used. The adapter gracefully falls back to the CBMS-first gate when enabled, avoiding unnecessary language model calls for questions already answered in knowledge chunks.",
  "control_plane/skills/forge_linux_verify.py": "This file implements the verification pipeline for Linux-targeted skills before they can be deployed to production nodes. When a new skill candidate is proposed through the forge system (identified with a linux.* prefix), the system runs four sequential checks: validation that the skill.json configuration file matches the required schema and declares a risk level, compilation of the Python handler using py_compile to catch syntax errors, sandbox execution where low-risk skills are run live against a test virtual machine via SSH while higher-risk skills only receive a dry-run echo command, and logging of the complete verification result to both the remote machine and a local record file. If any check fails, subsequent checks are skipped to fail fast. The entire verification report, including which checks passed and why any failed, is returned to the caller and stored in the forge verification log for audit purposes.",
  "control_plane/skills/goal_planner.py": "This file is the decision-making core where a small local language model (Phi-4-mini) selects and chains skills from a catalog to accomplish goals, operating entirely within constrained choice from a provided list with no hallucination of undefined skills. The planner implements multiple layers of caching before calling the expensive language model: first checking for exact-match recipes from previously learned solutions, second searching the CBMS knowledge database for similar goals with cached solution steps, third doing semantic recipe matching for paraphrased intents, and only if all caches miss does it query the language model. Each step has hard validation ensuring selected skills exist in the registry and input parameters match the skill schema, filtering out hallucinated placeholder values like \"<host_name>\" or empty strings. The goal execution function runs the planned skill chain through a risk gate, then if successful and learning is enabled, saves the goal-and-steps as a recipe for future reuse. All decisions about cache hits, LLM calls, and why chains succeeded or failed are logged and returned to the caller.",
  "control_plane/skills/papiery.py": "This file manages personal administrative items as JSON blocks stored directly on disk with no external database, following the same philosophy as CBMS where everything is a self-contained file. Each item (paper/task) is created with a title, optional deadline, notes, priority level (high/normal/low), and tags, and receives a generated identifier based on the title and creation timestamp. The system provides methods to list all open items sorted by deadline and priority, find items due within a specified number of days, search items by text in title or notes, and mark items as done with a completion timestamp. Stored items live in a directory on the user's E: drive and can be searched with command-line tools, backed up with file operations, and integrated into workflows as plain JSON.",
  "control_plane/skills/recipes.py": "This file stores learned success recipes for recurring problems as verified skill chains plus context about why they worked and the world state when they succeeded. A recipe is saved only after successful execution passes verification, capturing the goal, ordered steps, and reasoning snapshot. The system bridges recipes with the CBMS knowledge database by semantically indexing each new recipe in a Chroma session so that paraphrased goals (different words, same meaning) can match cached solutions without waking the language model, with a measured threshold of 0.70 confidence to avoid false matches. The core storage is a single JSON file tracked in git, with recipe lookup by an identifier derived from the goal text and reuse counting for statistics. Storing into Chroma fails gracefully without breaking the local JSON persistence.",
  "mcpServers/VS_CODE_MCP_CODEX/src/__init__.py": "This file is the main entry point that launches an MCP server with configurable transport (HTTP or stdio). It loads configuration from environment variables for logging level and port, then starts the server using either HTTP streaming mode on a configurable port or standard input/output mode depending on the transport_type parameter passed at startup.",
  "scripts/test_node_registry.py": "This smoke test validates the Faza 6 node registry system both as an in-process library and through an optional HTTP API. The in-process test registers nodes with capabilities and tools, verifies that nodes can be retrieved and listed, triggers a plan creation and execution to confirm the registry assigns appropriate machines to tasks, and checks that heartbeat updates work. The HTTP smoke test attempts to register a node via REST API and list all registered nodes, skipping gracefully if the API is unavailable. The test expects at least one local node to exist by default and validates the full lifecycle of node registration, discovery, plan creation, and execution.",
  "scripts/turbo_scanner.py": "This file is a fast project file scanner that leverages the Windows Everything CLI search tool to instantly locate files instead of slow directory traversal. It scans code, configuration, and documentation files across specified folders or the entire indexed system, computing metadata for each file including MD5 hash, line count, and parsed imports (Python and JavaScript). The system detects duplicate files by comparing hash values, builds a dependency graph by resolving import statements to actual files, identifies hub files that many others depend on, and marks orphan files with no incoming references. Results are saved as JSON files containing summary statistics, detailed file information, duplicate groups, dependency graphs, and a markdown report with tables and analysis. The tool runs file processing in parallel threads for speed and generates output suitable for codebase auditing and optimization.",
  "scripts/vault/_core.py": "This file implements AIONS local secrets storage using Windows DPAPI (Data Protection API) encryption for the current user account, with no external key management or cloud services. Encrypted blobs live in the runtime/secrets/vault directory (excluded from git), while the scripts that manage them are tracked. The system creates an operator marker on first use—a DPAPI-protected proof that a particular Windows user owns this vault—which is verified before any decryption to ensure the vault is only opened by the authorized user. Each secret is sealed by encrypting its content with DPAPI and an entropy value, then wrapping the ciphertext in a JSON envelope with metadata about the original filename, timestamp, and a unique vault identifier. The system maintains an index file listing all sealed secrets without exposing their plaintext. Sealing uses ctypes-based DPAPI calls with PowerShell fallback if ctypes fails, and all operations are offline with automatic file permissions set to owner-only.",
  "scripts/vault/open_secret.py": "This command-line tool decrypts and retrieves secrets from the AIONS DPAPI vault for the currently logged-in Windows user. It can decrypt a specific vault entry by identifier and write plaintext to a file with locked-down permissions (mode 0600), or list all vault metadata (identifiers, labels, sealed timestamps) without exposing any secrets. The script is meant to be run locally by automation or the operator; agents and integrations should read the resulting plaintext files rather than trying to echo secret content to chat. Decryption fails if the operator marker is missing or invalid, preventing access from other Windows users.",
  "server/models.py": "This file defines Pydantic data models representing the contract for a vector search and context storage system. It includes models for context items with optional IDs and metadata, batch operations adding multiple items to a session, search requests with query text and filtering options, search results returning scored hits with metadata, session management showing document counts and time ranges, and utility models for health checks and dashboard summaries. The models use type hints and field descriptions to enable validation and documentation of request/response payloads, particularly for an HTTP API serving knowledge retrieval and session management."
}
