[project]
name = "vs_code_mcp_codex"
version = "0.1.0"
description = "A simple MCP weather server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.23.0"
]

[project.optional-dependencies]
dev = ["debugpy==1.8.8"]