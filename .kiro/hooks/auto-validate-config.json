{
  "name": "Auto Validate Config on Save",
  "description": "Automatycznie waliduje składnię JSON/YAML przy zapisie",
  "enabled": true,
  "trigger": {
    "type": "onFileSave",
    "filePattern": "**/*.{json,yaml,yml}"
  },
  "action": {
    "type": "sendMessage",
    "message": "Zwaliduj składnię ostatnio zapisanego pliku konfiguracyjnego (JSON/YAML). Jeśli są błędy składni - pokaż je."
  }
}
